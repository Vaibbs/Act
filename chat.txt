apiVersion: v1
kind: Service
metadata:
  name: chat
spec:
  type: NodePort
  selector:
    app: chat
  ports:
    - protocol: TCP
      port: 5000       # Port exposed inside the cluster
      targetPort: 5000 # Actual port the container listens on
      nodePort: 32000  # Optional: Choice between 30000-32767. Left blank, K8s assigns one randomly.
